name: Pre Publish

on:
  push:
    tags:
      - 'v*.*.*.*'
      - '!v*'

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Create changelog
        run: echo "success"