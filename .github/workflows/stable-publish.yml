name: Stable Publish

on:
  push:
    tags:
      - v*
    tags-ignore:
      - v*.*.*-rc.*

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Create changelog
        run: echo "success"